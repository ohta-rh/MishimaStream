// Generated by CoffeeScript 1.3.3
(function() {
  var MishimaImage, MishimaStream, MishimaVideo;

  MishimaImage = Backbone.Model.extend({
    get_stream: function() {
      return [];
    }
  });

  MishimaVideo = Backbone.Model.extend({
    get_stream: function() {
      return [];
    }
  });

  MishimaStream = Backbone.Collection({
    initialize: function() {
      var stream;
      stream = [];
      MishimaImage.get_stream.each(function(image) {
        return item.to_json;
      });
      return MishimaVideo.get_stream.each(function(item) {
        return item.to_json;
      });
    }
  });

}).call(this);
